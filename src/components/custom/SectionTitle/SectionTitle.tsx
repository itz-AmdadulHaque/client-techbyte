import React from 'react'

const SectionTitle = ({ title }: { title: string }) => {
    return (
        <h3 className="font-bold text-5xl text-center font-[Candal] my-4">{title}</h3>
    )
}

export default SectionTitle